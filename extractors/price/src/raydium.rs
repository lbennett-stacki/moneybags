use solana_sdk::pubkey::Pubkey;

struct LiquidityPool {
    raydium_pool_address: Pubkey,
}

pub fn sync_raydium_pools() {
    todo!("impl")
}

pub fn get_liquidity_pools(_mint_address: &Pubkey) -> Vec<LiquidityPool> {
    todo!("impl")
}
