use std::error::Error;

fn main() -> Result<(), Box<dyn Error>> {
    println!("Welcome to moneybags");

    // TODO:
    // given a particular ticker, quantity and direction (buy/sell), execute a trade
    // ---
    // should calculate fees, bribes, slippage (w/ limit)

    Ok(())
}
